export enum ProjectStatus {
  InProgress = 'InProgress',
  Frozen = 'Frozen',
  Managed = 'Managed',
  Completed = 'Completed',
}

export enum ProjectStageEnum {
  KeysCollection = "KeysCollection",
  AdSetup = "AdSetup",
  SetupReady = "SetupReady",
  ManagedProject = "ManagedProject",
}

export enum DebtPercentageEnum {
  Half= 50,
  TenPercent = 10,
}