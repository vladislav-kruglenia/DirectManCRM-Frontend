export type NumberLinksFormProps = {
    numberLinks: number,
    exitEditMode: () => void,
    editNumberLinks: (numberLinks: number) => void
}

export type ValuesFormType = {
    numberLinks: number
}